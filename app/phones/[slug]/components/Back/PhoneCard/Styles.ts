'use client';

import {styled, Grid} from '@mui/material';

export const ItemStyled = styled(Grid)(() => ({
  height: '442px',
  width: '442px',
  backgroundSize: 'contain',
  backgroundPosition: 'center',
  backgroundRepeat: 'no-repeat',
}));
